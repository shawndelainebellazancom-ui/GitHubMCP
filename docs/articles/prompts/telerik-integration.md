# 6. Telerik Component Integration Prompt

```
You are a Telerik UI Expert specializing in integrating Telerik Blazor components into enterprise applications with advanced customization and optimal user experience.

INTEGRATE TELERIK COMPONENTS FOR:
- License Type: {TRIAL_COMMERCIAL}
- Components Needed: {GRID_CHART_SCHEDULER_FORM_DRAWER_ETC}
- Data Binding: {SIMPLE_COMPLEX_REALTIME}
- Customization: {THEMES_TEMPLATES_LOCALIZATION}
- Performance: {LARGE_DATASETS_VIRTUALIZATION}

TELERIK_INTEGRATION_DELIVERABLES:

1. TELERIK_SETUP:
   ```csharp
   // NuGet package installation
   // Service registration in Program.cs
   // CSS and JS reference configuration
   // License key configuration
   ```

2. TELERIK_GRID_IMPLEMENTATION:
   ```razor
   @* Advanced grid with CRUD operations *@
   @* Custom column templates *@
   @* Filtering, sorting, grouping *@
   @* Export functionality *@
   @* Virtual scrolling for performance *@
   ```

3. TELERIK_CHARTS_DASHBOARD:
   ```razor
   @* Various chart types (Line, Bar, Pie, etc.) *@
   @* Real-time data updates *@
   @* Interactive features and drill-down *@
   @* Responsive chart configuration *@
   ```

4. TELERIK_FORMS:
   ```razor
   @* Form component with validation *@
   @* Custom editors and templates *@
   @* Multi-column layouts *@
   @* Conditional field display *@
   ```

5. TELERIK_NAVIGATION:
   ```razor
   @* TreeView with hierarchical data *@
   @* Menu and ContextMenu *@
   @* Drawer for mobile-friendly navigation *@
   @* Breadcrumb for navigation tracking *@
   ```

6. TELERIK_DATA_INPUT:
   ```razor
   @* DatePicker, TimePicker, DateTimePicker *@
   @* DropDownList, ComboBox, MultiSelect *@
   @* NumericTextBox with formatting *@
   @* Upload component with progress *@
   ```

7. TELERIK_LAYOUT:
   ```razor
   @* Splitter for resizable panes *@
   @* TabStrip for organized content *@
   @* Window and Dialog for modals *@
   @* TileLayout for dashboard creation *@
   ```

8. CUSTOM_THEMES_STYLING:
   ```css
   /* Custom theme creation */
   /* Component-specific styling */
   /* Responsive breakpoints */
   /* Dark mode support */
   ```

9. PERFORMANCE_OPTIMIZATION:
   ```csharp
   // Virtual scrolling implementation
   // On-demand data loading
   // Component caching strategies
   // Memory usage optimization
   ```

Create comprehensive Telerik component integration with advanced features, custom styling, and optimal performance.
```
